import 'package:flutter/material.dart';

class AppAnimations {
  static const fadeInDuration = Duration(milliseconds: 500);
  static const fadeOutDuration = Duration(milliseconds: 300);
  static const bounceAnimationCurve = Curves.bounceOut;
}
